<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <p>Tak for dit valg af gave, <%= @customer.name %></p>

    <p>Du har valgt:</p>

    <p>
    <strong><%= @order.product.title %></strong><!--
    --><% @order.options.each do |option| %><%= ", #{option.option_array.text}: #{option.text}" %>
    <% end %>

    <% unless @customer.department.blank? %>
    <p>Afhenting af gave sker ved <strong><%= @customer.department %></strong></p>
    <% end %>
    </p>
  </body>
</html>
