<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <p>Tak for dit valg af gave.</p>

    <p>Du har valgt:</p>

    <p>
    <strong><%= @order.product.title %></strong><!--
    --><% @order.options.each do |option| %><%= ", #{option.option_array.text}: #{option.text}" %>
    <% end %>
    </p>
  </body>
</html>
